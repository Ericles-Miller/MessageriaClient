"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../../utils");
function default_1(type, templateConfig) {
    const { ts, framework = '' } = templateConfig;
    let extName = ['md', 'json', 'yml', 'js', 'jsx'];
    if (ts) {
        extName = [...extName, 'ts', 'tsx'];
    }
    if (['vue'].includes(framework)) {
        extName = [...extName, 'vue'];
    }
    if (type === 'prettier') {
        return (0, utils_1.stringify)({ [`*.{${extName.join(',')}}`]: 'prettier --write' });
    }
    if (type === 'eslint') {
        return (0, utils_1.stringify)({ [`*.{${extName.join(',')}}`]: 'eslint --fix' });
    }
    return '';
}
exports.default = default_1;
